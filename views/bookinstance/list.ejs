<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/bootstrapHead") %>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <%- include("../partials/sidebar") %>
            </div>
            <div class="col-sm-10">
                <h1><%= title %> </h1>
                <ul>
                    <% if(bookinstance_list.length === 0){ %>
                    <li>There are no book copies in this library</li>
                    <% }else{ %>
                    <% bookinstance_list.forEach(bookinstance =>{ %>
                    <li><a href="<%= bookinstance.url %>"><%= bookinstance.book.title %> :
                            <%= bookinstance.imprint %></a>
                        <% if(bookinstance.status === "Available"){ %>
                        <span class="text-success"><%= bookinstance.status %> </span>
                        <% }else if(bookinstance.status === "Maintenance"){ %>
                        <span class="text-danger"><%= bookinstance.status %> </span>
                        <% }else{ %>
                        <span class="text-warning"><%= bookinstance.status %> </span>
                        <% }if(bookinstance.status !== "Available"){ %>
                        <span>Due: <%= bookinstance.due_back_formatted %> </span>
                        <% }})%>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>
</body>

</html>