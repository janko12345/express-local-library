<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/bootstrapHead") %>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <%- include("../partials/sidebar") %>
            </div>
            <div class="col-sm-10">
                <h1><%= title %> </h1>
                <ul>
                    <% if(author_list.length === 0){ %>
                    <li>There are no authors</li>
                    <% }else{ %>
                    <% author_list.forEach(author =>{ %>
                    <li><a href="<%= author.url %> "><%= author.name %> </a>
                        (<%= author.date_of_birth_formatted ? `*${author.date_of_birth_formatted}` : "" %>
                        <%= author.date_of_death_formatted ? `| †${author.date_of_death_formatted}` : "" %>)</li>
                    <% }) %>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>
</body>

</html>